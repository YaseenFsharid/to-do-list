# To-Do List Application

This application is built using the following technologies:

- EJS
- CSS
- Node.js
- MongoDB
- Localhost

The application is deployable to the Heroku server.

To install the dependencies for the web application, run the following command:

```bash
npm install
